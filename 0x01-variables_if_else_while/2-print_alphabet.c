#include<stdio.h>
#include<string.h>
/**
 *main - prints the alphabet in lower case
 *
 *Return: SUCCESS if successful
 */
int main(void)
{
	char a;
	char z;

	a = 'a';
	z = 'z';
	while (some <= z)
	{
		putchar(a"\n");
		a += 1;
	}
	exit(SUCCESS);
}
