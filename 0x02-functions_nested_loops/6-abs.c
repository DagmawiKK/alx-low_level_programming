#include "main.h"
/**
 *_abs - finds absolute value of an integer
 *@int: the checked integer
 *Return: Always 0 (Successful)
 */
int _abs(int)
{
	if (int >= 0)
		_putchar(int);
	else
		_putchar(int * (-1));
	return (0);
}
